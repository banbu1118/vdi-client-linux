1.这是一个 Linux 版本的基于 Qt + FreeRDP 的 VDI 客户端，仅在 Debian13 上完成测试。

也可以使用 freerdp + qt 构建 qfreerdp 作为一个单独的rdp客户端。

2.安装教程

参考`安装教程.md`，可以编译 freerdp 和 qfreerdp。

参考`vdi-client-linux/tools/build-deb.sh`，可以编译 deb 包。

3.项目实现目标

参考`qt-qml-rdp-client-goal.md`
